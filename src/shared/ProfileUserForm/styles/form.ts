const padding = 20

export const styles = {
    bottomForm: {
        padding: `${padding}px ${padding}px 0`
    },
    buttonsBlock: {
        display: "flex",
        flexDirection: "column" as const,
        alignItems: "center"
    },
    submitButton: {
        marginBottom: 15
    }
}